function iterar(arg1) {

    for (let i = 0; i < arg1.length; i++){
        console.log(arg1[i])

    }

};


const numeros = [1, 2, 3, 4 ,5]
const nombres = ["Ignacio", "Lucas", "Francisca", "Alejandra", "Esteban"]

iterar(numeros);
iterar(nombres);
